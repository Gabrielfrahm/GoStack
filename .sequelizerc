const { resolve } = require('path');// importa o  resolve dentro do path para usar a pasta corretamente

// tem que ser importado no formato commonJS
module.exports = {
  config: resolve(__dirname ,'src' , 'config', 'database.js'),// caminho para o arquivo daabase.js
  'models-path': resolve(__dirname ,'src' , 'app', 'models'),//caminho para a pasta de models
  'migrations-path': resolve(__dirname ,'src' , 'database', 'migrations'),// caminho para a pasta migrations
  'seeders-path': resolve(__dirname ,'src' , 'databse', 'seeds'),//caminho para a pasta de seeds
};
